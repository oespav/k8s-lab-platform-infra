# Gateway API CRDs, pinned to the version agentgateway is tested against.
# Upgrade = change the version in this URL and commit.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.6.0/standard-install.yaml
