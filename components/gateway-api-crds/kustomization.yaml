# Gateway API CRDs, pinned to the version Istio 1.30 is tested against.
# Upgrade = change the version in this URL and commit.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.1/standard-install.yaml
