# https://traffic.internal.localhost via the internal gateway.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: traffic-console
spec:
  parentRefs:
    - name: internal
      namespace: infra-gw
      sectionName: https
  hostnames:
    - traffic.internal.localhost
  rules:
    - backendRefs:
        - name: traffic-console
          port: 80
