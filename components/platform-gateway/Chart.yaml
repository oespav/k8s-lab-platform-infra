apiVersion: v2
name: platform-gateway
description: Shared Gateway API gateways (agentgateway) owned by the platform team. App teams attach HTTPRoutes to them.
type: application
version: 1.0.0
appVersion: "1.5.0"
