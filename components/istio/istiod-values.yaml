# istio/istiod: the control plane. Sized down for a laptop.
# profile=ambient turns on ambient mode (ztunnel + istio-cni, no sidecars).
profile: ambient
pilot:
  autoscaleEnabled: false
  replicaCount: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
