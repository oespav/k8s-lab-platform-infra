# istio/ztunnel: per-node L4 proxy. Wraps every connection between enrolled
# pods in mTLS (HBONE) using each pod's SPIFFE identity, and enforces L4
# AuthorizationPolicies.
resources:
  requests:
    cpu: 20m
    memory: 64Mi
